var express = require('express');
  var router = express.Router();

  /* GET home page. */
router.post('/imgUrlData', function(req, res, next) {
        var str="";
     req.on("data",function(chunk){
         str+=chunk       })
     req.on("end",function(){
        console.log(str)
     })
     res.send({name:1});
 });
 module.exports = router;