const url = require("url");





module.exports = (req) => {


     return new Promise((resolve, reject) => {
        // 从 req 读取文件
         
        
        var query =    url.parse(req, (err, fields = {}, files = {}) => 
				{
            err ? reject(err) : resolve({fields, files})
        }).then(({files}) => {
				if (!(id in files))
				{
					console.log("错误")
				   throw new e(e)
				}
				
				response.writeHead(200,{'Content-Type': 'application/json'})
              
                response.end()

			}).catch(e => {
        if (e.statusCode === 413) {
            console.log("错误")
            throw new e(e)
        } else {
            throw e
        }
    });
		var params = JSON.stringify(query);

	});
}